# include_if_exists: includes file if it exists, silently skips if not.

base = value

# This file exists and should be included
include_if_exists '15_included.conf'

# This file does NOT exist — should be silently skipped
include_if_exists 'nonexistent_file_12345.conf'

final = done
