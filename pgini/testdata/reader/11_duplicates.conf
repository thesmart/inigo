# Duplicate keys: last wins.
# Duplicate sections: reopens the section.

key = first
key = second
key = third

[section_a]
dup = alpha
dup = beta

# Reopen default section
[default]
another = one
another = two

# Reopen section_a
[section_a]
extra = added
dup = gamma
